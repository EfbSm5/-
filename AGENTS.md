# RootPilot 仓库协作约定

## 开始工作

- 使用中文沟通。先读 `README.md` 了解项目，再按任务查看 `SPEC.md` 的当前状态与未验证项。
- 修改前检查分支、`git status` 和目标 diff；保留其他在途改动。未跟踪文件不等于可删除文件。
- 默认最小改动，不按任务关键词或具体应用增加导航特判，不顺手重构旧 Agent。
- `README.md` 说明用途与使用方法；`SPEC.md` 记录能力、验收证据、限制和后续计划；本文件只记录协作规则。

## 代码边界

- 主产品位于 `app/src/main/java/com/example/agent/rootpilot/`：Service 管理任务，AgentLoop 规划循环，ActionPolicy 校验动作，RootExecutor 执行。
- 本地待办复用 `agent/planning/FileTodoRepository`。旧 Agent 为实验入口，LiteRT-LM / Demo 后端不是 RootPilot 的默认运行路径。
- 保持截图上传授权、动作确认、输入目标绑定、输入法恢复和任务恢复的现有边界。不要把任务恢复当成凭据恢复，不自动重放可能已有副作用的动作。
- Token 仅通过现有 Keystore 加密配置存储处理；不得进入源码、日志、任务快照、截图或测试产物。不得读取个人桌面凭据或要求用户把 Token 发到聊天。
- 诊断优先使用 `RootPilotTrace` 的固定字段和原因码；不要记录任务正文、输入文本、待办标题、模型原始响应或异常原文。阶段进度不靠反复截图判断。

## 验证与审查

```sh
./gradlew :app:testDebugUnitTest :app:assembleDebug :app:assembleDebugAndroidTest :app:lintDebug --no-daemon
git diff --check
```

- 根据风险选择检查；最终区分静态审查、单测、构建、安装与真机验收，跳过或超时不算通过。
- 涉及跨层行为、状态、并发、生命周期、持久化、恢复、安全或核心链路时，使用 Writer + 独立 Reviewer。Reviewer 给出 PASS / FAIL / PARTIAL；无法独立审查时明确说明，不能用自查冒充。
- 发现问题先区分本轮回归、既有问题与未证实假设。没有证据不增加坐标补偿、等待、重试或兜底。
- 历史误点诊断已暂缓；没有用户新授权，不恢复调查。命令执行成功不等于目标页面发生变化。

## 真机安全

- 操作前核对当前授权设备的 serial、型号和连接状态；除发现设备外，所有 ADB 命令显式指定 `-s <serial>`。无线端口会变化，不硬编码历史地址，不自动切到另一台设备。
- 仅在任务授权范围内安装和操作。默认用非敏感只读用例，不发送消息、支付、删除数据或修改账号/系统设置。
- 不为连接方便执行全局 `adb kill-server`，不擅自关闭 GKD / Shizuku。遇授权或连接占用问题报告并等待用户处理。
- 用户输入凭据期间暂停截图与界面读取。页面证据只在确认目标或验收效果时获取；XML 导出失败不得读旧文件充数。
- 临时日志、截图及诊断产物放仓库外，报告路径和证据边界，不将其顺手提交。

## 交付

- 功能或验收状态变化时同步 `SPEC.md`，如实保留未验证项。
- 提交与 push 需要用户授权；只暂存本轮相关文件，不使用无差别 `git add .`，不夹带临时诊断和凭据。
- 完成时简述修改、审查、验证与剩余风险；若推送，报告提交号。
