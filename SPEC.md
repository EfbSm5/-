# RootPilot 产品与实施 Spec

更新日期：2026-09-24。主页与导航基线为 `95f1dd8`；本轮交付累计的聊天／设置整理、受限文件 Agent、工作区浏览与写入差异。历史小节保留当时的验证状态，不代表当前仍未交付。

本文件是项目状态与后续工作的入口。状态以代码、测试和真机证据为准；“计划”不代表已实现或已授权执行。

## 本轮：单目录文本文件 Agent

### 工作区浏览与写入差异（本地与小米核心用例通过）

- 工作区增加本地只读文件列表，目录优先排列，支持子目录、上级、刷新、UTF-8 文本预览。复用现有 SAF 访问与边界校验，不增加权限、删除／移动／重命名或直接编辑工具；文件 Agent 关闭时也能浏览，内容不自动进入聊天或模型。
- 浏览状态仅在内存中；关闭、离开聊天、更换／撤销目录及发送任务时清空预览并取消读取，递增请求版本拒绝迟到结果。正文与异常原文不进入日志／快照；失败仅展示固定原因提示。
- 写入确认新增有界行差异，显示删除／新增、旧新行号及 LF／CRLF／CR／无末尾换行；最多 300 行，省略时明确提示，保留完整前后文本切换。使用公共前后缀的线性算法，显示的是变更区段而非最小编辑序列。不修改确认 ID、提交前冲突检测和备份流程。
- 本地验证：337 项 JVM 测试通过（0 failures／errors／skipped），App 与测试 APK 构建通过，lint 0 errors／29 warnings，`git diff --check` 通过；日志 `/tmp/rootpilot-workspace-check.txt`。新增用例覆盖关闭后迟到结果、伪造列表项拒绝、目录往返、错误脱敏与恢复，以及中文、重复行、换行差异、超长前缀和输出预算；界面用例仅编译。
- 独立静态审查：按 `android-change-review` 核对本轮浏览状态、生命周期、SAF 调用链、无上传路径、差异边界和确认接线后 PASS。实现完成时仅通过本地检查，后续小米验收见下方补充，不沿用上一版本设备结果冒充本轮通过。

#### 小米验收与提交收尾

- 用户授权真机验收与提交推送后，再次核对小米 15 型号 `24129PN74C`、硬件 serial `12cd0365`，安装本轮 App 与测试 APK。4 项 instrumentation 通过（17.353 秒）：本地列表／预览／返回／关闭、Agent 关闭时仍可浏览、差异摘要／完整前后文本切换及确认 ID、生成中禁用管理入口。测试启动时仍需显式将 Activity 带回前台，不宣称无人协助启动稳定。
- 正式页面：Agent 保持关闭，工作区仍指向已授权专用目录；列出 3 份合成测试文件，打开 `acceptance.txt` 显示 `ROOTPILOT_TEST_BETA`。列表与短文本预览截图未见裁剪，未调用模型、写文件、读取其他个人文件或修改凭据。
- 337 项 JVM、双 APK 构建、lint 0 errors／29 warnings 和独立静态 PASS 沿用上一轮冻结代码的结果；本次仅补设备验收文档，提交前 `git diff --check` 通过。多级目录真实提供方、大文件性能、深色／大字体组合及差异视觉细节仍未完整验收；设备差异用例使用合成预览，不重放真实模型写入。
- 临时产物 `/tmp/rootpilot-workspace-device.0M0NeH/`（控件树、截图、`ui-tests.txt`）不入库。提交范围包含已完成的聊天／设置整理、文件 Agent、浏览与差异及相关测试文档；排除历史坐标诊断两个未跟踪文件和 `log/`，不删除它们。

### 文件 Agent 基线实现

- 用户授权实现：一个系统 SAF 目录，目录列表、文本读取、创建与精确编辑，每次写入展示前后文本并本机确认；不提供删除、Shell、Root、网络搜索或手机动作工具，不修改原手机任务协议。
- 目录 URI 与备份保存在私有 noBackup。系统目录授权与模型内容上传同意分开；文件 Agent 默认关闭，进程重启／API 配置保存或清除后关闭。切换文件模式、更换或撤销目录清空聊天；生成中不可变更目录或导出备份。
- 独立工具聊天协议处理完整 SSE tool_calls，聚合参数分片，保留工具轮 assistant reasoning_content，只在合法 finish + DONE 后交给执行循环。普通聊天／视觉动作仍拒绝 tool_calls。文件结果作为不可信资料，固定工具白名单与参数校验由本地执行，模型不能授权自身。
- 写入确认绑定不可变预览，提交前重新校验目录、文档身份和原文 hash；编辑前备份成功才写入，写入后回读。确认只能消费一次，拒绝终止本轮，取消清理未使用预览，失败不重放；已开始写入等待回执再结束，不承诺停止撤销副作用。
- 限制：UTF-8 text/*／JSON／XML，单文件 64 KiB、目录 100 项、每轮请求最多 8 个模型轮次／16 次工具，单次模型请求沿用 120 秒上限；不创建目录。原文备份达到 5 MiB 或 1000 项后拒绝需要备份的新写入，不自动清除旧备份。全部有效备份可列出并通过用户选定的系统文档位置导出，拒绝非文档 URI；卸载丢失本地备份。
- SAF 不保证跨应用原子性，provider 并发修改、部分写入、云端阻塞等不能由本地锁消除；不支持从属关系查询的 provider 拒绝使用。需实机确认系统授权和提供方行为，不能把纯逻辑单测等同于设备文件安全验收。
- 最终本地检查：314 项 JVM 测试通过（0 failures／errors／skipped），App／test APK 构建通过，lint 0 errors／29 warnings，`git diff --check` 通过。覆盖预览释放、拒绝／取消／迟到确认、冲突、备份失败、101 份备份完整可达及工具协议；新增 SAF、导出目标拒绝与界面 instrumentation 仅编译，未运行。日志：`/tmp/rootpilot-file-agent-reviewed-check.txt`。
- 独立审查：按 `android-change-review` 进行两轮审查，首轮发现导出 URI 缺少校验与第 101 份之后旧备份不可达，修复后第二轮静态 PASS。静态结论不替代真实提供方与模型验收。
- 初次实现按用户要求仅做本地检查；后续用户另行授权小米验收与合成测试内容上传，结果见下节。未提交或 push。

### 2026-09-24 小米文件 Agent 验收补充

- 目标身份：小米 15，型号 `24129PN74C`、硬件 serial `12cd0365`；安装 App 与测试 APK 成功，仅使用用户指定的无线 target。未操作其他设备、读取个人文件或修改凭据。
- 用户手动授权 `Documents/RootPilot-Acceptance-20260924-1700`，明确同意只上传这个专用空目录内的合成测试内容。真实聊天以高思考强度调用已配置 DeepSeek，通过工具创建 `acceptance.txt` 为 `ROOTPILOT_TEST_ALPHA`，读取后编辑为 `ROOTPILOT_TEST_BETA`；两次预览确认及实际文件回读通过。原文备份读取、实际 DocumentsProvider 创建 `original.txt` 并经生产导出函数写入 `ROOTPILOT_TEST_ALPHA` 通过。真实闭环 instrumentation 为 `OK (1 test)`。
- 另有 5 项设备用例通过：逐层重查目录、重复名／超限／不支持提供方拒绝、非法导出 URI 在打开流前拒绝、显式开启与生成中禁用管理、写入前后文本及确认 ID。首轮目录夹具的 `count` 被 `MatrixCursor.count` 遮蔽，限定为 `this@FixtureProvider.count` 后重跑 3 项安全用例通过，未修改生产存储逻辑。
- 首次 UI 用例在息屏锁定时失败；用户解锁后重跑通过。测试 Activity 启动仍需显式带回前台，不能宣称无人协助批量启动稳定。冷启动正式 App 后控件树显示文件 Agent 关闭；工作区仍显示已选目录和 `acceptance.txt` 原文备份入口。
- 真实验收测试默认跳过，只有显式传入与专用目录名一致的 `rootpilotFileAcceptance` 参数且工作区为空才运行。独立审查发现自动确认缺少目标路径断言，已在 Dialog 内同时核对文件名、修改前后文本后确认，第二轮静态 PASS；该增强断言已构建但未为此重放设备写入，设备成功证据来自增强前的测试版本。
- 本轮测试 APK 最终构建与 lint 通过（0 errors／29 warnings），`git diff --check` 通过；314 项 JVM 与 App 构建沿用上一轮，本轮生产代码未改。新增测试按 `android-change-review` 完成独立静态审查。
- 系统导出入口补验通过：从正式工作区的“导出 acceptance.txt”进入系统保存页，用户在同一专用目录保存 `rootpilot-original.txt`；重新核对小米身份后回读，该文件为 20 字节，内容严格为 `ROOTPILOT_TEST_ALPHA`，证明系统选择器返回后的实际导出结果与原文一致。
- 其他提供方、外部并发修改、部分写入及拒绝／取消的真实模型组合仍未完整验收。临时证据位于 `/tmp/rootpilot-file-acceptance.tslxiR/`，不入库；测试目录和原文备份保留，不自动清除。未提交、未 push。

## 本轮：聊天与设置视觉整理

- 聊天增加空态、分角色消息层次，思考内容仍折叠显示；四档思考强度、消息输入、字数、发送／停止收进底部输入卡。沿用原 Markdown、流式滚动和生成期间禁用规则，不改变请求、会话与存储行为。
- 设置首层保留 API 状态／配置／连接测试、应用选择和手动确认；系统权限与调试工具改为二级页面，顶栏与系统返回先回设置，再回进入前的任务／聊天。二级页恢复与任务忙碌门槛沿用页面状态管理。
- Token 仍不回显，不更改凭据、安全授权、任务执行、恢复和配置持久化；不升级依赖，不修改历史详情。
- 本地验证：274 项 JVM 测试通过，0 failures / 0 errors / 0 skipped；Debug App 与 instrumentation APK 构建通过，lint 0 errors / 28 warnings，`git diff --check` 通过。新增二级页恢复／逐级返回聊天与聊天空态切换用例，仅编译，未在设备运行。
- 独立静态审查：按 `android-change-review` 核对基线 `95f1dd8` 至本轮 7 文件最终差异及直接调用链后 PASS，未发现可确认缺陷。
- 验收边界：上一轮真机键盘／导航验收缺口仍保留；本轮未安装、未操作设备、未请求真实模型，不以静态检查替代深色、大字体、长 Markdown、键盘收放和停止按钮可达性的实际效果。本轮未提交或推送。

### 2026-09-24 小米界面验收补充（PARTIAL）

- 已核对小米 15（24129PN74C，硬件 serial `12cd0365`），安装最新 App 与测试 APK；仅操作此设备，未编辑真实凭据、请求模型、上传截图或执行真实任务。
- 5 项设备用例通过：浅色／深色各 1 项（320dp、1.5 倍字体的任务／设置／聊天布局和主题对比度）；正式 Activity 假数据二级页往返 1 项；聊天空态转消息 1 项；生成期间发送／思考强度／新对话禁用、思考折叠和停止回调 1 项。正式 Activity 用例启动期间曾回到系统设置，通过显式前台启动目标 Activity 继续，结果不代表无人协助批量启动稳定。
- 已查看本轮聊天浅色／深色、设置深色、权限与输入、调试工具截图：所检查区域未见按钮裁剪；320dp 大字体下清除配置文案换行，页面可滚动。假数据截图不证明真实模型请求或全部长 Markdown 排版。
- 未通过：键盘用例点击消息框后，5 秒内未检测到 IME 显示，因此尚未验证键盘开启时的底栏隐藏与发送／停止可达性；原因未确认，已请用户在正式 App 手动点击检查。
- 继续核验：同设备正式 Activity 在前台时系统报告 IME 未显示；新一次控件树导出 exit 137，未读旧 XML。将测试的语义点击替换为真实触摸事件进行单项对照，测试 APK 构建成功，仍在 5 秒 IME 显示等待处超时（`keyboard-touch.txt`、`touch-test-build.txt`）。对照未证明语义点击是根因，已还原该临时测试改动，不增加生产延时或强制弹键盘。正式界面手动点击结果尚未取得。
- 后续手动验收通过：用户在正式聊天页点击输入框后，同一小米系统读数 `mInputShown=true`；截图 `manual-chat-keyboard.png` 确认键盘已显示、输入框及发送按钮完整可见、顶栏保留、底部导航隐藏。输入为空时发送按钮禁用符合预期。此证据排除了“当前正式页面手动点击无法弹键盘”，但未确定自动化超时根因，不代表大字体／生成中停止按钮与键盘组合或键盘收起恢复已通过。
- 未完成：原 `settingsSubpagesRestoreAndReturnThroughSettingsToChat` 辅助 Activity 用例卡在启动阶段，主动终止目标进程；输出 `Process crashed` 属于该次人工终止，不作为产品崩溃证据。改用新 `settingsSubpagesReturnToSettingsAndThenChat` 直接 Activity 用例通过，但不替代状态恢复验收。
- 本次只新增直接 Activity 的导航测试并更新本文档，生产代码未改；测试 APK 再构建通过，`git diff --check` 通过。274 项 JVM、lint 和独立静态 PASS 沿用前一轮结果，未宣称重新执行。未提交、未推送。
- 证据：`/tmp/rootpilot-ui-acceptance.SYuQVY/` 内 `layout.txt`（3 项中 2 通过、1 键盘超时）、`navigation.txt`（主动终止）、`direct-ui.txt`（3 项通过）、`test-build.txt` 和本轮截图。临时产物未入库，可能被系统清理。

## 本轮：主页与导航视觉整理

- 基线 `460e223`。仅修改展示及页面导航，不修改 Service、AgentLoop、配置、截图上传、动作确认和恢复逻辑。
- 顶部改为标题与设置入口，任务／聊天放底部导航；设置返回其来源主页面，任务占用时始终回任务。历史页保留自己的逐级返回，不另设跳层级的顶栏返回。
- 主页使用引导标题、任务输入卡（截图授权、执行方式、开始）与状态卡；运行时状态／停止入口放前面。空闲状态不展示尚未开始的第 1 步。
- 键盘显示时隐藏底部导航，保留输入与已有聊天发送／停止能力。聊天生成、任务运行、配置编辑和恢复门槛保持不变。
- 设置内容分组、聊天消息样式和历史详情的全面视觉改版留待后续；本轮不增加功能、不升级依赖。用户已授权提交和推送，真机验收仍为下述 PARTIAL，不因交付改变结论。
- 最终本地检查：274 项 JVM 测试通过（0 failures / 0 errors / 0 skipped），Debug App 与 instrumentation APK 构建通过；lint 0 errors / 28 warnings，`git diff --check` 通过。
- 独立静态审查：按 `android-change-review` 复核最终补丁后 PASS；审查发现的历史页顶栏返回跳层问题已修复。静态结论不替代设备验收。
- 小米验收 PARTIAL：已确认设备型号 24129PN74C、硬件 serial `12cd0365` 并安装最终 APK。首次正式页面测试确认任务卡与底栏显示并留下截图，随后等待任务键盘弹出超时；该截图早于最终灰白背景调整，不能作为最终配色验收证据。第二次正式页面测试与单独旧 Miuix 键盘用例均在 `startActivitySync` 启动页面阶段超时，未进入界面断言，原因未确认；没有据此修改生产逻辑或系统设置。
- 尚未完成：最终配色、键盘收放、深色／大字体布局与本轮新增导航用例的真机验收。没有发送模型请求、上传截图或执行手机任务。设备尝试已停止，未把启动超时视为产品崩溃或通过。
- 本轮本地证据位于 `/tmp/rootpilot-home-ui.XlRa4o/`（`live-ui.txt`、`live-ui-final.txt`、`keyboard-fixture.txt`、早期 `task.png`）；临时文件可能被系统清理，不提交仓库。

## 目标与边界

在已取得 Root 权限的 Android 手机上，根据用户任务，通过截图与模型规划执行有限动作。首次保存 API 配置后可手机直连，不依赖电脑 Relay。

- 复用通用截图/动作协议，不按任务关键词或具体应用写导航特判。
- 截图及勾选的可启动应用名称、包名仅在用户允许上传后发给配置的 API；列表限制不意味着截图自动排除未勾选应用的内容。
- 支付、发送消息、删除、授权、账号与密码等敏感场景要求人工接管；这不是对模型识别准确性的保证。
- 打开应用、文本输入、系统按键始终确认；点击/滑动在手动模式确认，自动模式可直接执行。
- 不自动重放中断任务或可能已产生副作用的输入。

## 已实现

| 能力 | 当前行为 | 主要代码 |
| --- | --- | --- |
| 手机直连与配置 | 默认 `https://api.deepseek.com` / `deepseek-flash`，保留自定义地址、保存/更换/清除/测试连接 | `RootPilotApiConfigStore`、`RootPilotViewModel` |
| 凭据保护 | Keystore 密钥加密，密文位于私有 noBackup 目录；配置与任务恢复分离 | `RootPilotApiConfigStore`、`RootPilotRunStore` |
| 截图与规划 | Root 整屏截图，最长边缩至 1280，JPEG；模型每轮返回一个动作 JSON | `screen/ScreenshotProvider.kt`、`deepseek/DeepSeekClient.kt` |
| 动作约束 | JSON 解析、参数校验、步骤上限、相同帧/相同动作保护；坐标为全屏归一化 0–1000 | `action/`、`loop/AgentLoop.kt` |
| 应用启动 | 动态查询 MAIN/LAUNCHER，按用户勾选过滤，模型选择包名，执行前复查选择及入口，始终确认 | `apps/`、`root/RootExecutor.kt` |
| Unicode 输入 | 手动启用临时输入法；进程内 InputConnection 输入，支持中文/emoji/换行/标点，最多 128 UTF-16 单元 | `input/` |
| 输入安全与恢复 | 确认绑定具体 Editor/连接，焦点变化拒绝；拒绝密码框/自身编辑框；完成、取消后恢复原输入法，遗留记录可恢复 | `input/ImeTextInput.kt`、`RootPilotInputMethodService.kt` |
| 人工控制 | App、悬浮面板、通知确认；输入法面板提供本地文本预览并防截图 | `RootPilotService`、`ui/RootPilotOverlay.kt` |
| 本地待办 | `create_todo` 校验并始终确认，复用旧待办存储，同任务内防重复；多行预览可滚动 | `action/ActionParser.kt`、`loop/AgentLoop.kt` |
| 主入口 | 桌面启动 RootPilot，旧 Agent 保留为实验入口 | `AndroidManifest.xml`、`RootPilotActivity` |

代码路径均相对于 `app/src/main/java/com/example/agent/rootpilot/`。

## 本轮：本地任务历史与失败回顾

- 设置页提供历史入口，不增加主页操作按钮；列表展示时间、状态、累计耗时和步骤数，详情展示动作类型、确认事件、执行结果和固定失败原因。
- 只记录结构化脱敏字段，不保存任务正文、输入文本、待办标题、截图、Token、模型原始响应或异常原文。与恢复快照和凭据存储完全分开，不提供动作重放。
- 使用 noBackup 私有目录，最多保留 50 次任务，每次最后 256 条事件；纯聊天与 Root/截图调试不计入历史。旧版本记录不回填。
- 串行后台写盘，存储失败展示固定原因，不改变动作执行结果。清除成功后忽略旧任务后续事件，不停止任务，也不清除待办、配置与恢复快照；清除失败保留记录并提示失败。
- 进程中断可能丢失未落盘尾部事件；已落盘且未收尾的任务在下次进程读取时标记中断。最终状态由任务所有者收尾记录，不把停止请求或规划循环结束直接当成完整结束。
- 执行器返回成功不代表目标页面操作成功；模型报告完成不代表独立验收通过。中断记录也不能确认最后动作是否生效。
- 清除包含进程退出可能遗留的本功能临时副本；仅处理专属目录第一层自身命名文件，不递归删除，清理失败不报告成功。
- 本地验证：274 项 JVM 测试通过（0 failures / 0 errors）；Debug App 与 instrumentation APK 构建通过，lint 0 errors / 28 warnings，`git diff --check` 通过。覆盖保存/读取/清除、隐私字段、记录上限、中断恢复、停止收尾、存储失败不重试动作及清除不影响待确认任务。
- 独立审查：首轮发现临时副本遗漏清理，修复并增加四项用例后第二轮静态审查 PASS；修复后完整构建和单测已重新通过。
- 未验证：新增 5 项页面/导航 instrumentation 用例仅编译，尚未在小米运行；未验证设备上重启读取与清除的效果、列表/详情视觉布局。本轮未安装、不请求真实模型，未提交或推送。

### 2026-09-24 小米验收补充（PARTIAL）

当前结论（后续完成）：本轮 5 项页面/导航用例、1 项正式 Activity 只读导航，以及独立文件跨进程保存/读取/清除三阶段均通过。批量旧设置回归仍为 PARTIAL；不代表真实模型任务端到端验收。下文保留第一次受阻经过。

- 已核对小米 15（24129PN74C，serial `12cd0365`），通过用户提供的无线连接安装本轮 App 和测试 APK；没有操作另一台设备。
- 新增 `RunHistoryPersistenceInstrumentedTest`，使用独立 noBackup 测试文件、不覆盖用户真实历史。三个独立 instrumentation 进程依次执行保存、重启读取并清除、二次重启确认清除，均 `OK (1 test)`；验证失败原因/耗时保留和 RUNNING → INTERRUPTED 转换。测试数据已清除，不涉及配置、待办或真实模型调用。
- UI 验收未完成：安装前一次测试启动因测试 APK 尚未安装失败；安装后选定 12 项页面/导航用例，首项未进入测试页面，当前前台为其他应用。未读取该应用内容或截图；主动停止测试目标进程，因此该次输出的 `Process crashed` 是终止测试的结果，不作为产品崩溃证据。等待用户留出设备并确认 UI 自动化连接可用后继续，不擅自关闭 GKD/Shizuku。
- 测试 APK 新增用例构建通过，`git diff --check` 通过。结构化存储验证不替代生产历史页面、清除按钮和视觉布局验收；未提交或推送。
- 本轮原始结果位于 `/tmp/rootpilot-history-device.ee10wb/`，包含三个持久化阶段及 UI 启动尝试结果，目录可能被系统清理。

继续验收结果：

- 确认了小米的跨应用启动弹窗（Agent 打开测试 APK）；用户手动点“本次允许”后，4 项 `RunHistoryScreenInstrumentedTest` 全部返回成功，覆盖详情固定失败原因、返回、空列表、清除二次确认和中断提示。批量运行另有 1 项旧设置用例通过，随后测试 Activity 启动受阻而主动终止，不把该批次记成 12 项通过。
- 单独运行 `historyLivesInSettingsAndReturnsWithoutDiscardingTask`，`OK (1 test)`，完成本轮新增的第 5 项页面用例。
- 新增显式启用的 `RunHistoryLiveUiInstrumentedTest`，直接启动正式 RootPilotActivity，不经过跨应用辅助 Activity：设置 → 历史 → 返回设置 → 返回任务，`OK (1 test)`。只读真实历史，不清除、不编辑凭据；正式空列表截图 `real-history.png` 已检查，内容未见遮挡，清除按钮禁用符合预期。
- `uiautomator dump` 被系统终止（exit 137），未使用旧 XML；正式界面改用 Compose 语义断言与本地截图，不调用外部视觉模型、不关闭 GKD/Shizuku。尚不能据此认定 dump 被哪一个工具占用。
- 测试 APK 构建和 `git diff --check` 通过。本次仅增加验收测试与文档，未修改生产代码；原 274 项 JVM / lint 结果仍是上一轮证据，未宣称本轮重跑。没有提交或推送。
- 证据补充：`ui-tests-foreground.txt`（部分批量结果）、`history-navigation.txt`、`live-history.txt`、`real-history.png`，均位于上述临时目录。详情/清除使用测试数据验证，真实模型产生记录、非空正式列表的完整视觉验收尚未覆盖。

## 历史已验证基线

2026-09-19，提交 `6e9996c`：145 项 JVM 测试、11 项小米 instrumentation 通过；Debug/App 测试 APK 构建通过，lint 0 errors / 33 warnings，独立审查 PASS（限本轮范围和已验证设备）。

小米 15（型号 24129PN74C）手机直连真实模型：动态打开测试 APK，经确认在第一输入框写入一次“中文测试”，任务完成，常用输入法恢复；第二框和密码框未写入。早先曾错误聚焦第二框，不能称“未触碰”。

原始验收证据位于执行机器的 `/tmp/rootpilot-apps-unicode-20260919/`，不随仓库分发且可能被清理；以上是持久化摘要，不是全设备兼容承诺。

## 暂缓：定位偏差与重复聚焦诊断

状态：按用户决定暂缓，未复现、未修复。保留证据，不再继续追查或增加补偿逻辑。

已观察：首次点击落到第二框，后续模型自行纠正并多次重复聚焦，最终成功输入。现有证据不能证明是固定坐标偏移。

排查顺序：

1. 固定安全测试页、屏幕方向和设备身份，记录准确上传帧与尺寸。
2. 同步记录模型动作、归一化坐标、换算物理落点、目标控件边界与动作前后焦点。
3. 区分模型定位、缩放/换算、页面时序、浮窗拦截和模型冗余规划；不凭猜测增加偏移、等待或重试。
4. 仅在归因后提出最小修复，并补对应测试及真机复验。

诊断门槛：默认测试不得发送真实模型请求；显式启用的诊断仅操作无发送/保存功能的测试 APK，有限步骤，不输出凭据。诊断结束必须报告证据支持什么、仍未证明什么；单次成功不能视为稳定性完成。

以下诊断文件及运行命令仅记录本地历史取证，不纳入本次提交，干净检出不能直接运行。取证入口（本地测试 APK）：`input/CoordinateDiagnosticInstrumentedTest.kt`，不改变生产逻辑。安装包含该文件的测试 APK 后显式运行：

```sh
adb -s <已确认的小米serial> shell am instrument -w -r \
  -e coordinateProbe true \
  -e class com.example.agent.rootpilot.input.CoordinateDiagnosticInstrumentedTest \
  com.example.agent.test/androidx.test.runner.AndroidJUnitRunner
```

该参数代表本次诊断授权：读取手机已保存配置、上传当前测试页截图和应用列表，最多三步模型规划，仅允许落在测试页两个非密码框内的点击；文本、按键、启动应用等模型动作只记录而不执行。遇可重试请求失败等待 10 秒仅重试一次，再失败停止。测试进程会重启目标 App，不能与正在运行的任务同时执行。

结果返回 App 私有 `cache/coordinate-probe-*` 目录名，内含准确上传的 JPEG、动作 JSON 和尺寸/落点/前后焦点记录。模型响应和截图可能含屏幕内容，按敏感诊断产物处理，不进 Git。`OK` 仅说明取证流程结束，不能当成模型准确率通过；未传参数时跳过，不上传、不读取凭据。

完成标准：形成可复核的同帧证据；若是代码缺陷，修复前后对照与回归测试通过；若是模型误判，记录失败样本与下一步实验，不添加具体任务特判。

### 2026-09-20 取证进展

- 同一小米，物理屏幕 1200×2670，实际上传 JPEG 575×1280。模型 `tap(435,129)` 经现有策略换算为 `(521,344)`；第一框边界 `[32,208][1168,486]`，落点在框内。执行后第一框获得焦点，第二框未聚焦。
- 下一帧模型直接返回 `type(中文测试)`，诊断按约定未执行输入。该样本没有复现偏移或重复聚焦，不能据此关闭历史问题。
- GKD 普通无障碍服务保留运行；用户关闭 UI 自动化模式后专用连接占用消失，取证流程可以运行。这不是完整 GKD 共存或所有规则组合验收。
- 首轮产物：执行机器 `/tmp/rootpilot-coordinate-20260920/`，包含两帧准确上传 JPEG、原始动作 JSON 和前后焦点。测试页之外的浮窗仍可能进入截图；产物不入库。
- 首轮诊断未运行完整生产 Service/确认流程；后续已完成带确认浮窗及键盘前后的单次同帧对照，见下方生产补充取证。仍不先修改坐标公式或增加等待/重试。
- 本轮最终本地检查：145 JVM tests / 0 failures / 0 errors，App 与测试 APK 构建成功，lint 0 errors / 33 warnings；新增诊断默认调用实际返回 assumption skip。独立静态审查 PASS。
- 最终测试版本补充“其他高层窗口遮挡落点则停止”检查（测试页上实际存在通话浮窗），以及缩小上下文的一次网络重试。检查到点击之间仍有时间窗口，不保证绝对无误触；遮挡拒绝分支未在真机上刻意触发。
- 最终版本复跑：首个请求超时，等待 10 秒并缩小至 fixture 上下文重试一次；重试长时间未返回，整轮超过五分钟后人工终止测试进程，未继续请求。栈位于 `HttpDeepSeekClient` HTTP 响应体读取处。结果为 **PARTIAL**，不是测试通过，也不是坐标故障。`final-attempt/` 保存部分证据；instrumentation 的 `Process crashed` 是本次主动终止的结果。
- 目前步骤数上限与 HTTP 读超时不是严格的整轮截止时间；诊断需人工时间预算。若后续要加入总请求时限，应作为独立问题取证和测试，不在本轮顺手重构网络层。

### 生产 Service 同帧补充（2026-09-20）

新增默认跳过的 `input/ServiceCoordinateInstrumentedTest.kt`。它观察真实 Service/AgentLoop，不替换模型客户端，也不自动批准动作。启用方式：

```sh
adb -s <已确认的小米serial> shell am instrument -w -r \
  -e serviceCoordinateProbe true \
  -e class com.example.agent.rootpilot.input.ServiceCoordinateInstrumentedTest \
  com.example.agent.test/androidx.test.runner.AndroidJUnitRunner
```

- 仅在没有运行或待恢复任务时执行；使用已保存 API 上传安全测试页和真实应用列表。外部操作者核对当前页面、落点与遮挡后，通过真实浮窗确认安全点击。遇待确认的非 tap 或终态停止；生产 Wait 和解析失败重试仍保留，测试不新增重试。
- 最多观察三次确认，180 秒仅为观察期限；finally 请求停止并有界检查原输入法及恢复记录。停止状态和输入法恢复不证明阻塞 HTTP 已退出。
- 私有缓存保留实际规划 JPEG、解析后的动作坐标或文本长度，不保存原始 HTTP JSON、reason、文本或配置。截图仍是敏感产物，不进 Git。默认不传参数时跳过，不读取凭据、不上传。
- 真机观察流程 50.153 秒完成：`tap(487,137)` → `(583,365)`，位于第一框 `[32,208][1168,486]`；真实浮窗确认后第一框聚焦，第二框和密码框未聚焦。下一张实际规划帧包含键盘且不含 RootPilot 确认浮窗，下一动作是长度 4 的 type，未批准、未写入。
- 结束时 STOPPED、原小米输入法恢复且恢复记录清空。此次流程未复现坐标偏差或重复聚焦，不代表历史问题已解决。
- 首张实际规划帧可见应用启动动画的圆角与背景，说明截图可能早于动画完成；本次点击正确，尚不能将它归因为历史故障，未据此新增等待。
- 产物：执行机器 `/tmp/rootpilot-service-coordinate-20260920/`，包含前后 XML/截图与 `final/cache/service-coordinate-8554883078679094100/` 的两张规划帧、脱敏动作记录。原始产物不随仓库分发。
- 构建、JVM 测试与 lint 通过；独立静态复审 PASS。总的历史问题归因仍为 PARTIAL；按用户决定不继续补失败样本或调查启动动画。

## 未完成验收：真实应用只读搜索闭环

通过 RootPilot 打开系统设置，搜索“深色模式”，读取搜索结果并汇报结束。只搜索和读取，不切换开关，不更改设置，不发送消息。使用现有应用目录、中文输入和人工确认机制，不新增应用专用导航逻辑。

2026-09-20 真机结果：PARTIAL。模型完成打开设置、从原无线调试页返回三层、滚动到首页顶部；两次搜索栏点击后仍停在首页，随后人工停止，未输入文本、未读取搜索结果、未改设置。模型坐标对应物理点约 `(599,592/595)`，XML 搜索节点范围 `[168,503][1164,635]`，仅能证明范围包含落点，不能证明触摸已被目标消费或判定具体原因。按用户决定，不继续展开误点调查，不添加补偿。

无线调试及开发者页未显示确认浮窗，这三次 BACK 经操作者逐项核对后调用现有 Service CONFIRM 入口；其余操作经实际浮窗确认。因此本次不是完全脱离电脑的交互验收。停止后原小米输入法保持不变。证据位于 `/tmp/rootpilot-settings-search-20260920/`，不进 Git。继续中文输入与结果读取前，需人工打开系统设置搜索页；完成后应明确标记人工接管，不能把分段通过描述为全自动闭环通过。

### 2026-09-21 发布后只读搜索复验

功能提交 `73984de` 已推送。仅操作已确认的小米 15，本轮仍为 **PARTIAL**：真实模型提出 `open_app(com.android.settings)`，经电脑辅助调用既有确认入口后，日志成功且前台实际进入 `.MainSettings`。随后确认一次搜索栏 `tap(499,202)`，日志记录物理落点 `(598,539)` 及命令成功；下一轮模型再次提出相近搜索栏点击，页面仍为设置首页，未批准第二次点击，按暂缓约定停止。未执行中文输入、读取搜索结果或修改设置，不能称完整闭环通过，也未据此确认误点原因。

进度来自 `RootPilotTrace`；UiAutomator 导出退出137，未读取旧 XML，仅在确认目标及核对最终页面时截图。停止后日志确认 `run_end=cancelled`，原小米输入法保持不变。证据在 `/tmp/rootpilot-search-20260921/`，不入库。本轮未更改产品代码，沿用功能提交之前通过的167项单测、构建、lint及独立审查结果。

## 当前实现：本地待办与主入口

- RootPilot 接入 `create_todo`，复用旧 Agent 的 `FileTodoRepository` 和私有 `agent_todos.json`，不迁移或清空已有数据，不通过 Shell 写入。
- 标题和 RFC3339 截止时间复用旧协议校验；无截止时间用 null/省略。保存前始终确认，展示标题及截止时间；拒绝或确认前取消不写入。
- 同次任务已成功保存的相同标题/截止时刻不重复写入，重复确认同一动作只执行一次。不同任务可创建相同待办；不提供跨进程 exactly-once 保证。写入中断后沿用人工恢复，不自动重放。
- 桌面启动入口改为 RootPilot，保留“旧 Agent（实验入口）”。旧端侧 LiteRT-LM 与 Demo 后端保留原处，不迁入 RootPilot，也未移除其依赖。
- 本地保存不需要额外网络请求，但模型规划仍沿用截图授权、Root 截图和 DeepSeek 请求；不宣称完整流程离线或无 Root 可用。待办不会创建系统闹钟或通知提醒。
- 本地验证：156 项 JVM 测试通过，App/测试 APK 构建成功，lint 0 errors / 33 warnings；独立静态复审 PASS。
- 小米真机：新增 launcher 与18行待办滚动预览两项 instrumentation 实际通过，未跳过；新入口解析及冷启动到 RootPilot，点击旧入口实际到 MainActivity。UiAutomator dump 两次被结束（退出137），未读取旧 XML，后续界面验证使用显式小米 serial 的截图及 Activity 归属，不据此修改产品逻辑。
- 真实 DeepSeek 返回 `create_todo`，实际浮窗确认“RootPilot待办验收0921／无截止时间”后仅保存一条；下一轮模型 finish，界面“已完成”。进程停止并重启后文件仍是该一条。另一次带绝对截止时间的待办预览后点击停止，文件逐字节未变化，取消项未写入，原输入法未改变。验收待办保留在手机上，无系统提醒。
- 证据目录 `/tmp/rootpilot-todo-20260921/`，含 instrumentation、入口/预览/完成截图和重启／取消后的测试数据；不进 Git。确认入口由电脑辅助点击，不能声称此次验收完全无电脑。写入瞬间进程死亡与跨任务去重不在验收范围；旧搜索闭环仍为 PARTIAL，误点诊断仍暂缓。

## 当前实现：结构化执行日志

目的：从日志判断任务所在步骤及等待原因，避免依赖反复截图读取状态；界面是否确实发生变化仍需单独验收。

- 固定 Logcat tag `RootPilotTrace`，每行 JSON，字段包含 `runId / step / event / stage / status / elapsedMs / actionType / result / reasonCode`。`step` 从0开始（任务开始前为-1），`elapsedMs` 为单调时钟累计耗时，阶段耗时可由开始/结果两条相减。关联单次任务与步骤，记录截图、模型请求、协议解析、人工确认、动作执行及任务结束。
- 只记录固定事件、状态、耗时、动作类型及必要数值；不记录 API 配置、Token、任务正文、待办标题、输入正文、模型 reason/message、原始响应或异常原文。
- 点击坐标来自规划动作及实际执行参数；命令成功不等于页面变化。本地待办写入结果与界面动作结果分别标识。
- 停止请求与执行协程结束是两个事实，不能用前者推断 HTTP 已退出。不改变模型重试、动作确认或执行策略。
- 复用 App 内存日志展示，最多保留500条；Logcat 非持久化审计存储，不新增文件日志或上传。同步日志 sink 异常被隔离，不将已经完成的副作用变成执行失败；真正的协程取消保持原有检查。
- 本地验证：167 项 JVM 测试通过，App/测试 APK 构建与 lint 通过（0 errors / 33 warnings）；独立静态复审 PASS。
- 小米真机日志验收（2026-09-21）：真实模型返回 `wait` 并执行成功；`create_todo` 进入等待确认后取消，依次记录 `stop_requested` 与唯一的 `run_end=cancelled`，未执行写入，待办文件与验收前一致。Root 检查的开始和结果使用同一 `runId`。18 条 JSON 记录通过字段白名单、关联和终态检查，测试标题未出现在日志中。验收使用日志判断进度，没有读取截图；证据位于 `/tmp/rootpilot-trace-20260921/`。
- 本轮真机未逐一触发点击坐标、网络失败和待办保存日志；这些日志路径有单元测试覆盖，不代表相关页面效果或所有设备场景已验收。`control` 记录独立的单次服务操作，不要求具备任务循环的 `run_start/run_end`。

开发时读取近期执行日志（只用已确认的小米 serial，不清空全设备日志）：

```sh
adb -s <已确认的小米serial> logcat -d -v raw RootPilotTrace:I '*:S' | tail -n 300
```

先按 `runId` 选择本次任务，再看最后一个事件。Logcat 可能轮转、进程可能被系统终止；没有终态日志只能说明缺少结束证据，不能直接当成仍在执行或已成功。

先过滤 tag 再限制输出行数；不要用 `-t 300` 代替管道尾部过滤，设备系统日志密集时会漏掉目标 tag 的近期事件。

## 当前调整：紧凑悬浮面板与项目文档

- 根目录 `AGENTS.md` 记录协作、验证和安全边界；`README.md` 介绍功能、使用方式、构建及已知限制。本文件继续记录当前状态与后续计划。
- 悬浮面板宽度由260dp缩至220dp，普通动作默认显示一行摘要，点击标题可展开完整详情；按钮触控区域保留至少48dp。保留拖动、确认和停止，不修改动作策略或恢复机制。
- 待办及人工接管信息保留完整可滚动预览；输入正文仍在原有防截图输入法面板展示，不进入普通浮窗。
- 待办及人工接管等完整预览收起时禁用浮窗确认，新动作或新步骤会重置预览。输入浮窗仅显示字符数与安全预览提示，不显示正文或 reason。读屏摘要使用实际文本，标题提供步骤、状态和展开状态。
- 本地验证：168项 JVM 测试通过（无失败、错误、跳过），App/测试 APK 构建通过，lint 0 errors / 32 warnings。小米15最终版4项 instrumentation 实际通过，覆盖220dp宽度、至少48dp触控区域、展开不确认、确认前移除浮窗、长内容滚动、收起禁确认、新动作预览重置、Type正文不泄漏和停止回调。测试不请求真实模型、不创建待办。
- 证据：`/tmp/rootpilot-compact-20260921/`。独立终审 PASS；真机测试使用 View 属性及 `performClick()`，不代表拖动手势、TalkBack朗读或不同字体/方向的完整视觉验收。新版已安装。
- 提交前复验：全量构建检查通过，168项单测、4项小米面板测试均通过、无跳过，lint仍为0错误/32警告；手机已安装App及测试APK的SHA-256与当前构建产物一致。复验产物位于 `/tmp/rootpilot-compact-push-20260921/`，不随仓库分发。

## 当前实现：允许启动的应用

- 设置页提供应用列表，可按名称/包名搜索、勾选和全不选，修改自动保存，首次默认空；升级不自动沿用此前可启动的全部应用。
- 包名集合存储于应用私有 noBackup 目录，与API配置、任务快照分离。文件为空、缺失、损坏或不可读时不授予任何启动权限；保存失败显示错误，不宣称已保存。撤销保存失败时，旧的已保存授权仍可能有效。
- 模型每轮获得过滤后的应用目录，动作校验只接受目录中的应用；Root执行器在启动命令前再次读取最新选择并核验入口。确认前撤销会拒绝尚未执行的启动，不承诺撤回已经提交的系统启动命令。
- 选择按包名持久化，卸载的应用不会出现在目录中；同包名重装且仍被勾选时重新可用，不提供签名或工作资料级隔离。新安装的其他包名默认不允许。
- 此功能只限制 `open_app`，不限制前台截图、点击桌面图标、链接跳转、返回键或其他动作，不宣称完整应用访问隔离。原截图授权、人工确认和输入法保护不变。
- 本地验证：177项 JVM 测试全部通过，无失败、错误或跳过；App/测试APK构建成功，lint 0 errors / 32 warnings。覆盖缺失/损坏拒绝授权、跨实例持久化/清空、原子保存失败、实时目录过滤、确认期间撤销后拒绝启动、模型请求仅含勾选项及下一轮响应撤销。
- 首轮小米真机：生产新版已安装；初版ViewModel测试通过保存、新ViewModel读取、清空、保存失败提示及任务授权不变，使用临时目录未改用户应用选择。当时新增多编辑器测试已编译，因设备离线未在真机复跑；后续补验见下方。
- 首轮UI自动化验收为 **PARTIAL**：Compose测试在Espresso 3.5.1初始化时因系统缺少 `InputManager.getInstance()` 失败，尚未进入列表交互；未扩范围升级测试框架。当时指定小米无线连接离线，列表视觉/真实交互及生产进程重启验收未完成；后续补验见下方。未请求真实模型或启动真实应用。证据：`/tmp/rootpilot-allowlist-20260921/`，不入库。
- 首轮独立终审：代码 PASS，整体验收 PARTIAL；无已确认的生产代码阻断项，后续补验见下方。

### 2026-09-21 重新连接后的补验

- 同一小米15重新连接后，最终ViewModel instrumentation实际通过（1项、code 0），包含多编辑器从磁盘增量保存、重建读取、清空和保存失败提示；对应日志 `instrumentation-vm-final.log`。
- 生产界面通过ADB辅助核验：列表首次为0；勾选一个应用后私有文件只保存该包名；强制结束并冷启动后界面计数仍为1、重新打开列表仍勾选。搜索名称片段“12306”仅显示对应应用；过滤状态下点“全不选”会清除全部选择，而不是仅清除当前搜索结果。再次冷启动后界面计数为0、文件为 `[]`。测试勾选已撤回，未启动任何被选应用、未发模型请求、未创建待办，原小米输入法未变。
- UiAutomator仍退出137，未使用旧XML；界面核验使用指定设备的新截图与存储读回。上述生产列表核心路径及重启持久化缺口已补齐，但Compose自动化仍受原Espresso兼容问题阻塞；包名搜索首轮ADB字符输入受当前输入法转换影响，未取得该分支的有效真机通过证据。未扩展修复输入法或测试框架。
- 页面证据位于原验收目录的 `picker-reopen.png`、`selected.png`、`restarted.png`、`search-filtered.png`、`cleared-restarted.png`，不进Git。补验未改生产代码，功能与上述验收边界一并交付。
- 独立补验记录核对 PASS；代码审查维持 PASS。整体验收仍为 PARTIAL，仅剩上述包名搜索真机分支与Compose自动化未通过，不再将核心列表路径和重启持久化列为待验。

## 当前调整：任务主页与设置分离

- 主页聚焦任务输入、截图上传授权、开始/停止、执行状态及动作确认；中断任务仍需人工恢复或放弃。
- API 配置、允许启动的应用、悬浮窗与输入法权限、手动确认模式移至设置；Root 检查、截图、单步执行、截图预览、日志及旧 Agent 入口收至调试工具。
- 页面切换不保存或丢弃 API 编辑草稿，不改变任务与凭据存储。配置编辑未完成时主页提示回设置处理；Token 编辑继续使用现有防截图保护。
- 本轮仅调整界面组织，不修改模型请求、动作策略、应用授权、输入法恢复或历史误点逻辑。独立代码审查 PASS。
- 本地验证：177 项 JVM 测试通过，无失败、错误或跳过；App/测试 APK 构建通过，lint 0 errors / 32 warnings，`git diff --check` 通过。
- 新增4项页面导航 instrumentation，覆盖设置返回、编辑状态提示、忙时撤销入口与停止回调、安全输入预览、页面状态恢复及中断任务入口。小米实际运行4项均因既有 Espresso 的 `InputManager.getInstance()` 兼容问题失败，未进入交互，不能算通过；尚未覆盖真实编辑草稿内容、防截图窗口标志和长动作详情的完整真机验收。
- 新版已安装至已核对身份的小米15，冷启动与主页截图验证完成；设置页交互仍未验收。UiAutomator 导出退出137，未读取旧XML。视觉工具虽在连接时指定小米，后续调用未保留设备绑定而误读另一设备截图，随后模型鉴权失败，未执行点击；已停止该工具路径，该结果不计入小米验收。未改用户凭据或应用选择，未请求 RootPilot 模型任务。
- 本轮证据位于 `/tmp/rootpilot-home-settings-3eh0Rk/`；`home.png` 为显式小米 serial 获取的主页证据，`build.log` 和 `navigation-tests.log` 记录本地检查与测试阻塞。视觉工具产物不作为目标设备证据，不入 Git。整体验收为 PARTIAL。

## 当前工作：任务结果与停止链路

- 主页结果卡片区分模型报告、本次执行完成、失败、停止与人工接管；模型 Finish 不作为目标页面操作成功的独立证据。结果仅保留在进程内，不新增历史库、截图存储或任务重放。
- 本地待办存储返回成功后发布保存回执，展示本轮已保存标题与截止时间；后续失败或停止仍保留这些回执，新任务清空。缺少回执不能证明写入未发生，尤其是落盘与取消竞争时，需要人工核对，不自动重试。
- 停止先进入 STOPPING，取消当前工作并保留任务占用；任务完成退出后才进入 STOPPED。停止期间不能启动第二个任务，迟到阶段事件不能覆盖停止状态。尚未退出时保留中断快照，避免进程被终止后误认为动作未发生。
- 网络等待使用可取消的 HTTP 调用与单请求总时限，不新增业务重试；原凭据脱敏、拒绝重定向和自定义 API 地址边界保留。此时限不是整个多步任务的总时限。
- 为解决已复现的 `InputManager.getInstance()` 反射异常，测试依赖更新为 Espresso 3.7.0 / AndroidX JUnit 1.3.0（[官方修复记录](https://developer.android.com/jetpack/androidx/releases/test)）；不升级产品 Compose 等依赖。网络实现显式依赖 OkHttp 4.12.0，限于现有客户端传输实现。
- 旧 HTTP 实现3项本地回归测试全部复现超时：等待响应头/正文时 `cancelAndJoin` 超过1秒，持续滴流时调用方500毫秒超时未在1.5秒内退出。修复后15项网络定向测试通过，覆盖取消、连接关闭、120秒默认总时限的短时测试、拒绝重定向、不自动重试和错误脱敏；证据 `/tmp/rootpilot-network-evidence.kBZMfK/`。
- 首轮全量检查：194项 JVM 测试通过，无失败/错误/跳过；App和测试APK构建通过，lint 0 errors / 31 warnings。小米15上4项非页面 instrumentation通过：真实Android本地HTTP的header/body取消与滴流总时限；隔离Service夹具的STOPPING占用、快照保留、迟到事件隔离、完成后STOPPED及保存回执保持。Service夹具不等于生产Service完整端到端验收，不执行Root动作或真实模型请求。
- 页面测试越过原Espresso初始化异常，但首条测试未进入可验证页面；当时前台是其他应用，原因未确定，已主动终止本App测试进程。尚未将设置交互及结果卡片真机页面验收标记为通过，等待设备环境确认。证据在 `/tmp/rootpilot-results-stop-HF84yQ/`，页面测试 `Process crashed` 是主动终止结果，不是已确认的产品崩溃。
- 最终复验：194项 JVM 测试、App/测试APK构建与lint再次通过，最终APK已安装；4项小米非页面测试再次通过（`native-final.log`，0.689秒）。独立代码审查 PASS，整体验收仍 PARTIAL。无凭据、任务正文、截图或模型原始响应新增到持久化结果存储或日志。
- 用户确认手机可用于测试后再试Compose交互：首项仍未返回有效断言，系统日志显示测试Activity启动后出现小米跨应用启动确认页、测试辅助EmptyActivity启动/退出及Activity销毁超时；尚未证明具体归因。再次主动终止本App测试进程，未修改系统权限/GKD/Shizuku；不将其标记为产品崩溃或通过。已回到真实RootPilot主页，设置与返回可由人工补验。结果卡片页面、自然拒绝入口的Service直接时序及真实模型端到端仍未验证。
- 用户随后在小米生产界面手动确认：设置中的API、应用选择、权限、调试分组显示正常，点击“返回任务”可正常回主页。此项人工补验通过；未进入Token编辑，不代表草稿/防截图或结果卡片自动化分支通过。最终主页截图 `home-final.png` 使用显式小米serial取得。

## 当前工作：文字聊天与流式输出

- 新增独立文字聊天入口，复用保存的 API 配置；支持多轮、停止生成、新对话与关闭/低/高/最高思考强度。仅发送已完成问答的正文作为上下文，不发送思考内容、取消或失败的半成品；不使用 Root、截图和设备动作。与设备任务分开，生成中不能切换并启动任务或编辑 API 配置。
- 保存/清除配置成功后重置聊天；发送前再次核对端点、凭据和模型是否变化，变化则隔离旧会话，不把旧账号/端点的正文带给新配置。取消配置编辑不清空聊天。
- 思考强度对应 DeepSeek `none/low/high/max`，关闭时禁用 thinking，其余启用；只作用于聊天，不改变现有设备任务思考设置。聊天输出预算 16384 tokens，任务保持原 4096；两者沿用单请求 120 秒时限，不自动重试。[官方接口约定](https://api-docs.deepseek.com/api/create-chat-completion/)。
- 流式显示正文 Markdown 和可折叠思考。使用原生 TextView Markdown 渲染，不使用 WebView，不加载远程图片、不自动打开链接；不引入网页端 Streamdown。会话只保留在 ViewModel 内存，不写任务快照、日志或历史文件；新对话清空，进程退出丢失。输入限制 16000 字符，已完成上下文限制 64000 字符，会话最多 100 条消息；达到上限提示新建，不静默丢弃上下文。
- 任务循环接收流式进度，悬浮窗默认仅显示阶段，主动展开可查看模型思考和动作草稿，各段只展示末尾 4000 字符。预览窗口防截图；抓屏前仍同步隐藏。半截 JSON 从不进入动作解析或执行，只有完整流正常结束后的响应才走原校验/确认链路；中断、截断或格式错误停止，不执行草稿。
- 预览仅驻留内存，不进入通知、结构化日志或恢复快照；阶段变化清空，确认期间使用原有动作详情，保持截图上传授权与输入目标/输入法恢复约束。
- 本轮按用户要求仅做本地测试、构建、lint 与独立审查，不连接或操作真机；真实 DeepSeek 流式响应、不同思考强度表现、Markdown 排版/滚动、悬浮窗预览和 Android 生命周期交互均待实测，不将本地通过等同于真机通过。
- 本地全量验证通过：227 项 JVM 测试，0 失败/错误/跳过；其中新增流式网络 13 项、聊天控制 13 项、任务流式门槛 3 项、悬浮预览 4 项。App 和测试 APK 构建通过，lint 0 errors / 32 warnings，`git diff --check` 通过。新增聊天/Markdown 页面 instrumentation 已编译但未运行。日志：`/tmp/rootpilot-chat-stream-vn9umf/final-checks.log`。
- 独立 Reviewer 最终代码审查 PASS，并核对测试 XML、构建/lint 日志及 diff。配置变更隔离问题已修复并覆盖测试；本轮无安装/设备操作，整体验收仍为 PARTIAL，未提交或推送。
- 自定义 API 需支持 Chat Completions SSE；仓库现有开发 Relay 原样转发但会缓冲上游完整响应，因此可用性仍待联调、不承诺经过它实时显示输出。本轮未改 Relay，也未引入流式失败后的自动回退请求。

### 2026-09-21 小米真机补验

- 用户随后授权真机验收。设备经 manufacturer/model/serial 核实为 Xiaomi / 24129PN74C / `12cd0365`，本次连接 `10.93.133.30:33807`；所有设备级命令显式绑定该连接，未操作另一台设备。新版 App 与测试 APK 安装成功，真实 RootPilot 主页冷启动与视觉确认完成。
- 4 项原生 instrumentation 通过（0.573 秒）：Markdown 标题/列表/引用/代码块、未闭合代码块增量更新与链接不外跳；既有悬浮窗尺寸、展开/收起、敏感动作预览、确认及停止回调。它们是原生控件夹具，不等于真实任务流式悬浮窗端到端通过。
- 新增显式 opt-in 的 `DeepSeekLiveChatInstrumentedTest`，默认跳过，仅传入 `rootpilotLiveChat=true` 才调用真实 API。测试在手机进程内复用生产 ChatController/HttpDeepSeekClient 和 Keystore 配置读取，要求官方 HTTPS 端点及 deepseek-flash，不修改保存配置，不上传截图或执行 Root 动作。最多4条固定非敏感文字请求，记录阶段/增量次数，不记录凭据、提示词或模型原文；退出时有界等待请求清理。
- 手机真实直连测试通过（1项、2.974秒）：关闭思考时4次不同输出快照；高强度3次快照并正确保留多轮标记；最高强度3次快照且计算答案正确；低强度收到第1次输出后取消，5秒内退出、回复保留未完成状态，新对话清空成功。高/最高均观察到非空思考字段，关闭档无思考字段。低档只验收到流和取消，不代表低档完整回答已验。
- Compose聊天页面测试在第1条启动阶段未进入可验证页面，约40秒后主动终止本App测试进程；`Process crashed` 是主动终止结果，不作为产品崩溃证据。控件树导出退出137，未读取旧XML，未使用可能绑定错设备的视觉交互路径。实际聊天页点击、长回复滚动、配置切换/旋转交互，以及真实任务悬浮窗流式预览仍待验，整体验收保持 PARTIAL。
- 新增测试APK构建与lint通过；新增测试安全审查及真机证据独立复核 PASS，整体验收边界维持 PARTIAL；本轮无产品代码变更。证据 `/tmp/rootpilot-chat-device-w0naip/`：`native-render.log`、`live-chat.log`、`chat-ui.log`、`live-test-reviewed-build.log`、`home.png`。验收后回到真实RootPilot主页，未改变用户配置或系统设置，未提交/push。

### 同日继续补验：聊天页面与真实流式预览

- 用户要求继续完成页面验收，另明确同意只上传测试页、不得执行手机动作。保持同一小米 serial；将页面测试改为 `createEmptyComposeRule` 配合直接启动真实 RootPilotActivity，避开 ActivityScenario 的辅助 Activity。先确认本 App 前台后测试能够推进；不据此推断之前卡顿的唯一系统原因，不修改系统权限或 GKD/Shizuku。
- 首轮6项页面测试通过；最终最新APK上的7项全部通过（15.984秒）：4项 ChatScreen 测试涵盖长内容滚动时新增回复不抢回底部、旧半成品标识、生成期间按钮约束、思考展开/收起、无配置禁发；真实页面验证发送非敏感文字至 DeepSeek、展示回复、思考折叠、新对话、收到正文/思考后通过按钮停止、生成时禁止导航以及任务/聊天/设置往返。
- 真实 Activity 重建后，聊天页、同一 ViewModel 与已完成消息保持；不是进程死亡持久化或横屏兼容结论。聊天截图 `live-chat.png` 已人工视觉检查：标题、列表、引用、代码块、输入框、强度选择与发送区域正常，未见重叠或异常裁剪；图片只包含本 App 测试聊天布局。
- 用户授权的单张截图验收通过：截取实际显示的静态 TextView 自身，不含系统或其他应用窗口；经生产 AgentLoop → HttpDeepSeekClient → RootPilotOverlay 获得5次不同流式快照，展开的原生面板文字随输出更新，确认始终禁用、窗口保持 FLAG_SECURE、结束后移除。日志记录 captures=1、requests=1、blocked_requests=0、execute_attempts=0，模型直接完成，无任何手机动作或待办写入。
- 为遵守“只上传测试页、不执行动作”，此项使用禁止动作的执行器及测试页截图源，不运行生产 Service 生命周期或全屏 Root 抓屏/动作注入；不能把这次通过写成全部设备执行链路验收。当前新增聊天及模型输出预览的上述约定用例已通过，历史误点、跨厂商、横屏/多窗口、进程死亡恢复仍是独立范围。
- 全量227项 JVM 测试、App/测试APK构建、lint及 `git diff --check` 通过；最终去重计数补丁后测试APK与lint再构建通过。独立审查通过后执行真机验收。证据 `/tmp/rootpilot-ui-complete-c2ud17/`：`chat-complete.log`、`final-device.log`、`final-build.log`、`final-reviewed-build.log`、`live-chat.png`。新版已安装并回到真实主页；未改用户配置，未提交/push。

## 当前工作：任务运行边界收口

- 从 Service 提取可独立测试的 `RootPilotRunController`，负责任务启动、占用、停止/销毁收尾、动作确认、恢复与循环事件处理；Service 保留 Android 入口、依赖组装、通知和悬浮窗宿主。不拆 Gradle 模块，不改动作协议或旧 Agent。
- 进程内 `RootPilotTaskState` 同时保存状态与当前运行所有者。Service 销毁后取消工作、解绑窗口宿主，但不提前释放任务占用；网络/输入法清理结束后才释放。重建实例在此期间不能启动或覆盖旧任务；中断任务保留恢复记录并等待人工处理，不自动重放。无任务快照的单次诊断退出后为已停止。
- UI 新增运行占用标记，完成/失败事件尚在收尾时仍禁用新任务与配置修改。正常停止继续保留快照至退出；旧宿主不再收到迟到的界面/通知更新，已成功保存的待办回执仍可保留。
- 快照读取区分“文件缺失”和“损坏/IO 失败”，写入及清除错误使用无原始 cause 的固定异常。任务快照保存失败即中止后续规划或动作；读写/清除失败提示人工核对，不自动重试；当前进程必须显式成功放弃记录后才能开始新任务。已经执行的动作不会撤销。
- 保持截图上传授权、通知确认 token、输入目标绑定、输入法恢复及凭据/任务恢复分离。API 配置与任务配置的类型拆分、旧端侧依赖隔离不在本轮。
- 新增13项 JVM 控制器用例及10项存储错误用例，并将原停止 instrumentation 从反射修改 Service 私有字段改为直接运行控制器。覆盖取消清理、销毁/重建、恢复阻断、写入前失败、完成清除失败、确认 token、迟到待办回执，以及快照失败被输入法清理异常覆盖时仍保持阻断。Android 控制器夹具不等于系统 Service 销毁端到端验收。
- 最终全量250项 JVM 测试通过（0失败/错误/跳过），App与测试APK构建通过，lint 0 errors / 32 warnings，`git diff --check` 通过。独立审查首轮指出错误锁存时机与清除测试分支问题，修复后第二轮 PASS。证据：`/tmp/rootpilot-architecture-reviewed-check.log`；本轮未安装或运行真机 instrumentation，系统 Service 销毁、通知/悬浮窗与真实输入法恢复需另行验收。
- 首次全量检查有1项未修改的 SSE 测试返回网络失败，单次定向复跑及随后两轮全量均通过；尚未确定首次失败原因，没有修改网络代码或增加重试。首次记录保留于 `/tmp/rootpilot-architecture-check.log` 和 `/tmp/rootpilot-architecture-stream-first-failure.xml`。本轮架构变更尚未提交或推送。

## 已交付：设置页 Miuix 试点

- 用户授权试用 Miuix 并进行必要依赖升级；只改设置页的分组卡片、按钮、开关、局部主题和应用选择弹窗外观。主页、聊天页不迁移组件；不改 API 保存、模型请求、Root 执行、权限与动作确认逻辑，不扩大架构重构。
- 固定使用 `top.yukonga.miuix.kmp:miuix-ui:0.9.4`。其发布 AAR 要求 compileSdk 37；升级 Kotlin 2.4.20、Compose BOM 2026.09.00、AGP 9.1.1、Gradle 9.3.1，并按官方方式迁移 AGP 内置 Kotlin。minSdk35、targetSdk36保持，不添加忽略依赖元数据检查的开关。
- 实际依赖解析：AndroidX Compose1.12.1、CMP1.12.0、Kotlin stdlib2.4.20、serialization1.7.3。Miuix 的传递依赖引入 Material3 **1.5.0-alpha22**；因此未改 UI 代码的主页、聊天和旧 Agent 也受底层依赖变化影响。这是实验性试点，不宣称全部页面已完成兼容验收。
- 设置主题只在设置页及应用选择弹窗生效，跟随系统明暗模式。Token 保留原密码输入框/遮罩和 Activity 防截图逻辑；弹窗使用原生 Compose Dialog（显式 SecureFlagPolicy.Inherit）承载 Miuix Card，不将凭据移入库的弹窗或本地持久化状态。所有配置、权限、应用选择、调试入口复用原回调与启用条件。
- 本地验证：依赖解析、AAR 元数据检查通过；250 项 JVM 测试全部通过（0 failures/errors/skipped），App 与测试 APK 构建通过，lint 0 errors / 28 warnings，`git diff --check` 通过。日志 `/tmp/rootpilot-miuix-build.JlEN20/`、`/tmp/rootpilot-miuix.gT32bR/full-check.log`。新增设置开关回调/密码语义、运行收尾期间禁用设置变更的 instrumentation 测试已编译，尚未运行，不能计为通过。
- 真机验收发现并修复两处主题接入问题：Miuix 卡片外缺少默认内容色导致深色标题黑字；Material Scaffold 默认背景与 Miuix 卡片同色。显式提供内容色及设置页容器颜色，主页颜色不变。
- 小米15（24129PN74C，设备号12cd0365）最终版本6项测试通过：真实任务/聊天/设置导航，以及5项 fixture 测试，覆盖开关单次回调、空 Token 密码语义、执行中与收尾期间禁用配置编辑、假应用搜索/选择/清空，以及320dp宽、1.5倍字体下明暗主题颜色与滚动可达性。测试使用内存假配置，无模型请求、Root动作或凭据修改。最终截图已目视确认明暗标题可读、卡片与背景区分，应用选择弹窗无明显裁切。大字体下顶栏“设置”会换行，后续整体导航迁移时改善布局。
- 旧测试宿主首次未正常进入前台，后续直接启动真实 Activity 并由 ADB 显式带到前台完成测试；早先中断和连接断开的运行不计通过。截图/XML/日志位于执行机器 `/tmp/rootpilot-miuix-device.smqaNa/`，远端临时 XML 为 `/sdcard/miuix-20260921-smqaNa-home.xml`、`/sdcard/miuix-20260921-smqaNa-settings.xml`。未触碰另一台已连接设备。
- 独立审查 PASS（限静态审查与本地检查），已覆盖主题修正和新增测试。最终本地检查仍为250 JVM通过、App/测试APK构建成功、lint0错误/28警告；日志为上述目录的 `settings-final-build.log`、`final-settings-tests.log`。fixture 不证明生产防截图生命周期；实际 FLAG_SECURE、防截图弹窗、横屏/多窗口与旧 Agent 兼容仍未覆盖。
- 依赖依据：[Miuix](https://github.com/compose-miuix-ui/miuix)、[AGP9.1兼容表](https://developer.android.com/build/releases/agp-9-1-0-release-notes)、[内置Kotlin迁移](https://developer.android.com/build/migrate-to-built-in-kotlin)、[Kotlin兼容表](https://kotlinlang.org/docs/gradle-configure-project.html)。

## 已交付：RootPilot 页面 Miuix 迁移

- 用户授权在设置页验收推送后迁移其他页面；范围为任务主页、聊天页、任务结果/确认/恢复卡片和顶栏。`RootPilotTheme` 统一上述页面的明暗主题，旧 Agent 实验入口不变。独立原生悬浮窗及输入法面板保留，避免将页面视觉迁移扩大为窗口生命周期重写。
- 任务和聊天使用 Miuix 输入框、按钮与卡片，上传授权开关保留原回调；当前导航/思考强度有选中语义和主色，导航与标题分行以适应窄屏。密码输入框保留 Material 安全输入组件，Markdown 继续使用现有禁止外部链接/图片加载的渲染器，并共享配色。
- 不改模型请求、配置存储、任务状态、动作确认或聊天滚动跟随逻辑。新增内存 fixture 覆盖中文/emoji/换行输入、上传与确认回调计数、隐私文本隐藏、思考档位选中，并扩展320dp/1.5倍字体的主页和聊天截图。
- 页面迁移初版全量本地检查通过：250 JVM tests、App/测试APK构建、lint0错误/28警告；小米12项页面测试通过，覆盖上述fixture、聊天滚动/生成状态和真实导航。明暗截图复核完成；一次深色聊天截图局部缺绘在单独复跑中未复现，尚未归因，不添加延迟或生产兜底。
- 实际空聊天弹键盘发现顶栏被平移出屏、发送与键盘之间留白；窗口取证为 `sim={adjust=pan forwardNavigation}`。旧 Manifest 同样未指定模式，尚未完成旧版对照，不能认定为 Miuix 回归。用户明确同意本轮一起修正，为 RootPilotActivity 显式指定 `adjustResize`，保留现有 `imePadding()`。新增真实键盘测试发现320dp/1.5倍字体时发送按钮被说明区挤出屏幕（13项中1项失败）；据此在键盘显示时收起聊天说明/新对话区域，输入视图最多显示3行，键盘收起后恢复5行，草稿不截断。保留强度按钮、错误提示和发送/停止门槛。
- 最终键盘适配通过全量本地构建、250 JVM测试和lint（0错误/28警告）；小米13项页面回归全部通过，包含真实键盘、320dp/1.5倍字体、五行emoji草稿完整性、导航/发送/停止可见性及单次停止回调。真实空聊天页面额外取证确认 `adjust=resize`，顶栏保持可见，发送与键盘之间的大空白消失；未发送消息。独立审查PASS，已复核最终diff、新主题文件、构建/测试报告与生产截图/XML，结论限本轮及已测场景；真实模型、FLAG_SECURE、其他设备/键盘、横屏多窗口未重验。
- 本轮最终证据位于 `/tmp/rootpilot-miuix-device.smqaNa/`：`pages-compact-ime-build.log`、`pages-final-tests.log`、`pages-keyboard-fixture-final.png`、`final-keyboard.png`。远端 XML 另有 `/sdcard/miuix-20260921-smqaNa-pages-{home,chat,keyboard}.xml`、`/sdcard/miuix-20260921-smqaNa-final-{home,chat,keyboard}.xml`（花括号为三个实际文件名的缩写）及 `/sdcard/miuix-20260921-smqaNa-handoff.xml`。截图显示已有其他应用通话悬浮层，未关闭或修改；不承诺其遮挡下所有控件均可点击。最终已将手机切回新版任务主页。
- 本轮不调用真实模型、不修改凭据或执行手机任务；fixture/布局检查不是完整任务验收。页面迁移与键盘修复随本次提交交付，不包含历史坐标诊断文件或本地日志。

## 后续候选计划（未完成）

1. **GKD 共存验收**：分别验证 RootPilot 正常运行与 UiAutomation 测试连接。此前已注册的 UiAutomation 曾阻断验收工具；尚不能推断所有 GKD 模式都影响 RootPilot。
2. **真实应用安全用例**：打开应用 → 在搜索框输入中文 → 读取结果；不发送消息，不更改账号或系统设置。需先选定目标应用和非敏感内容。
3. **恢复与兼容扩展**：输入待确认时进程退出后的恢复实测、横屏/多窗口/工作资料与其他厂商设备。现有单机及单测结论不覆盖这些场景。
4. **模型请求时限验收**：单请求总时限与取消正在实现验证；真实服务端故障、DNS/TLS 异常及整个多步任务的总时限不在当前验收范围。

暂不扩展多模型路由、配置架构重构、后台无人值守敏感操作或按应用维护固定坐标。

## 验证与维护约定

```sh
./gradlew :app:testDebugUnitTest :app:assembleDebug :app:assembleDebugAndroidTest :app:lintDebug --no-daemon
```

设备命令必须显式 `adb -s <已确认的小米 serial>`，无线地址每次重新确认，不写死到源码。输入法启用和首次 Token 输入由用户完成；输入凭据时暂停界面读取与截图。

- 行为变更同时更新本文件的“已实现 / 当前工作 / 后续计划”，附提交、验证范围和未验证项。
- 跨层、安全、生命周期及恢复行为需要独立 Reviewer；没有独立审查能力时明确说明。
- 原始截图、模型响应与设备日志放任务专用临时目录，不进入 Git；Token 不进入日志、任务快照、源码或 spec。
- 文档修改不等于提交/push 授权；仅提交已验证且获授权的范围，保留无关工作区修改。
